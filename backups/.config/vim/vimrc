" vimrc — auto-read by Vim >= 9.1.0327 from $XDG_CONFIG_HOME/vim/vimrc when no
" ~/.vimrc or ~/.vim/vimrc exists (which is the case here). Its only job is to
" keep Vim's runtime state out of $HOME and under XDG dirs.

" Runtime/pack paths so plugins and spell files live under XDG too.
set runtimepath^=$XDG_CONFIG_HOME/vim
set runtimepath+=$XDG_DATA_HOME/vim
set runtimepath+=$XDG_CONFIG_HOME/vim/after
set packpath^=$XDG_DATA_HOME/vim,$XDG_CONFIG_HOME/vim
set packpath+=$XDG_CONFIG_HOME/vim/after,$XDG_DATA_HOME/vim/after

" State (created on demand) — this is what stops Vim writing ~/.viminfo etc.
set backupdir=$XDG_STATE_HOME/vim/backup | call mkdir(&backupdir, 'p')
set directory=$XDG_STATE_HOME/vim/swap   | call mkdir(&directory, 'p')
set undodir=$XDG_STATE_HOME/vim/undo     | call mkdir(&undodir,   'p')
set viewdir=$XDG_STATE_HOME/vim/view     | call mkdir(&viewdir,   'p')

if !has('nvim')
    call mkdir($XDG_STATE_HOME . '/vim', 'p')
    set viminfofile=$XDG_STATE_HOME/vim/viminfo
endif
