# wgetrc — loaded via WGETRC=$XDG_CONFIG_HOME/wget/wgetrc (set in exports.sh).
# Keeps wget's HSTS cache out of $HOME (~/.wget-hsts) and under XDG state.
# wget expands a leading ~ in this value.
hsts-file = ~/.local/state/wget/hsts
